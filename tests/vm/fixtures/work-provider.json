{
  "name": "vm-work-provider",
  "operator": "alice",
  "purpose": "a lab vm",
  "work_runtime": true,
  "provider": "fixture",
  "vetoes": {
    "never_do": "never enact without oracles",
    "networks": "local",
    "remotes": false
  },
  "fixture_turns": {
    "responses": [
      {"send": "fixture-complete"}
    ]
  },
  "commands": [
    "answer purpose a lab vm",
    "answer work-runtime yes",
    "answer operator alice",
    "answer never-do never enact without oracles",
    "answer networks local",
    "skip remotes",
    "view envelope",
    "view accept",
    "accept"
  ],
  "os_commands": [
    "view login",
    "quit"
  ],
  "expect": [
    "own fixture token isolation",
    "work uid cannot read the OS token",
    "L-16",
    "L-17",
    "no live Grok token required"
  ]
}
