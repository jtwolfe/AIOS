# Connectors

---
name: connectors
description: Prefer MCP connectors over the browser. Discover, then call.
---

If a Connector exists for a service, use it. Browser is fallback.

- Discover schema, then call.
- Auth is a connect card authored by the host. Do not paste authorization
  links into chat. Do not ask for tokens in chat.
- Newly installed connectors are available on the next message, not the
  same one.
