# Jobs

A job is a file. It is not a person.

Each entry is **path + slice + skill + state**. Address it that way.
Do not store a personality, a voice, an avatar, or a memory-of-self.

Handoff is operational: paths, oracles, last evidence. Not a psyche.
